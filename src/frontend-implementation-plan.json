{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "CalendarView heading: add one-sentence description under title",
  "requirements": [
    {
      "id": "REQ-14",
      "summary": "Add a short, muted, single-sentence description directly beneath the CalendarView h2 heading text without changing any other elements.",
      "acceptanceCriteria": [
        "The selected h2 renders a primary heading line and a second line of smaller, muted description text immediately beneath it.",
        "The description is a single short English sentence explaining what the calendar/journey page does.",
        "Only the selected h2 element is modified; no other layout elements (e.g., button placement/size) are changed in this build."
      ],
      "file_operations": [
        {
          "path": "frontend/src/components/CalendarView.tsx",
          "operation": "modify",
          "description": "Modify only the existing <h2 className=\"text-3xl font-bold text-foreground\">…</h2> element to render the heading text plus a second line beneath it (e.g., a child <span> styled as smaller and muted). Ensure the description is one short English sentence explaining the purpose of the calendar/journey page, and do not alter surrounding layout or the Compare Progress button."
        }
      ]
    }
  ]
}